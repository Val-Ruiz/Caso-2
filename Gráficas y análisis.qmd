---
title: "Gráficos y análisis"
format: pdf
editor: visual
---

```{r}
library(scales) #Ajustar las gráficas
library(car) #Manejar datos estadísticos 
library(tidyverse) #Conjunto de paquetes
library(emmeans) #Calcular medias ajustadas
library(ggplot2) #Creación de gráficos 
library(ggpubr) #Mejorar gráficos 
library(corrplot) #Hacer mapas de clasificación 
theme_set(theme_pubr())
```

```{r}
datos <- read.csv("Datos.csv")
view(datos)
colnames(datos)
```
En el siguiente código se plantea contar que materiales usa cada país y posteriormente sumarlos para sacar un promedio según la región en la que se encuentra. Con el objetivo de ver que regiones usan una mayor variedad de recursos para representarlos en una gráfica.
```{r}
Diversidad <- datos %>%
  rowwise() %>% #Aplicar funciones fila por fila (país por país)
  mutate(Diversidad = sum(c_across(c("Biomass", "Excavated.earthen.materials..including.soil..nec", "Fossil.fuels", "Metal.ores", "Mixed.and.complex.products.nec.", "Non.metallic.minerals", "Products.from.biomass", "Products.from.fossil.fuels", "Products.from.metals", "Products.from.non.metallic.minerals", "Waste.for.final.treatment.and.disposal")) > 0, na.rm = TRUE)) %>% 
  #"mutate" crea una nueva variable de nombre "Diversidad", que contiene el conteo.
  #"c_across" seleccionas las columnas que tienen distintos tipos de materiales.
  #"> 0" si el país usa ese material, si el valor es mayor que cero, entonces da TRUE, si no, da FALSE.
  #"sum(..)" Cuenta cuantos materiales si fueron usados.
  #"na.rm" ignora los NA.
  ungroup () %>% #Trabaja con los datos completos sin agrupamientos.
  group_by(RegionWB) %>% #se agrupan los países según su región.
  summarise(Diversidad = mean(Diversidad, na.rm = TRUE)) %>%
  arrange(desc(Diversidad)) #Calcula el promedio de los materiales usados entre los paises de cada región 
Diversidad
```

Diversidad alta significa que los países de esa región usan muchos tipos diferentes de materiales (como biomasa, metales, combustibles, etc.). Caso contrario a los paises con baja diversidad, ya que usan menos materiales, es probable que sea porque su economía se centra únicamente en ciertos materiales.

```{r}
# Creación del gráfico de barras para visualizar el promedio de materiales que usa cada región.
ggplot(Diversidad, aes(x = reorder(RegionWB, Diversidad), y = Diversidad)) +
  geom_col(fill = "darkgreen") +
  labs(title = "Promedio de materiales usados por región", x = "Región", y = "Promedio")
```

Creación de gráficas para mostrar la distribución total de detrminados materiales que han sido utilizados por cada región.

Distribución de biomasa.
```{r}
Biomasa_region <- datos %>%
  group_by(RegionWB) %>%
  summarise(cantidad = sum(Biomass, na.rm = TRUE)) %>%
  arrange(desc(cantidad))
Biomasa_region

ggplot(Biomasa_region, aes(x = reorder(RegionWB, cantidad), y = cantidad)) +
  geom_col(fill = "green") +
  coord_flip() +  # Para que las regiones estén en el eje Y
  labs(
    title = "Biomasa por región",
    x = "Región",
    y = "Biomasa"
  ) +
  scale_y_continuous(labels = comma) + 
  theme_minimal()
```
En el conjunto de datos, las columnas extraidas de la base de datos "Flujos de Materiales Globales" (GMF) que representan flujos de materiales (como biomasa, combustibles fósiles, minerales, etc.). 
Estos flujos pueden ser: 
Positivos → cuando un país produce, importa o acumula biomasa.
Negativos → cuando un país exporta, pierde o consume biomasa.

Entonces, si un país tiene biomasa negativa, y se sumas todos los países de una región, el total puede dar negativo.

```{r}
ggplot(datos, aes(x = Biomass, y = EPI, color = RegionWB)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, color = "gray") +
  labs(
    title = "Relación entre la Biomasa y el EPI",
    x = "Balanza de biomasa",
    y = "Índice de Desempeño Ambiental (EPI)"
  ) +
  theme_minimal()
```

Distribución de energía.
```{r}
Energia_region <- datos %>%
  group_by(RegionWB) %>%
  summarise(cantidad = sum(Energía..GWh., na.rm = TRUE)) %>%
  arrange(desc(cantidad))
Energia_region

ggplot(Energia_region, aes(x = reorder(RegionWB, cantidad), y = cantidad)) +
  geom_col(fill = "blue") +
  coord_flip() +  # Para que las regiones estén en el eje Y
  labs(
    title = "Energía por región",
    x = "Región",
    y = "Energía"
  ) +
  scale_y_continuous(labels = comma) + 
  theme_minimal()
```

```{r}
ggplot(datos, aes(x = Energía..GWh., y = EPI, color = RegionWB)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, color = "gray") +
  labs(
    title = "Relación entre la Energía y el EPI",
    x = "Balanza de Energía",
    y = "Índice de Desempeño Ambiental (EPI)"
  ) +
  theme_minimal()
```

Distribución de combustibles fósiles.
```{r}
Combustible_region <- datos %>%
  group_by(RegionWB) %>%
  summarise(cantidad = sum(Fossil.fuels, na.rm = TRUE)) %>%
  arrange(desc(cantidad))
Combustible_region

ggplot(Combustible_region, aes(x = reorder(RegionWB, cantidad), y = cantidad)) +
  geom_col(fill = "black") +
  coord_flip() +  # Para que las regiones estén en el eje Y
  labs(
    title = "Combustible por región",
    x = "Región",
    y = "Combustible"
  ) +
  scale_y_continuous(labels = comma) + 
  theme_minimal()
```

```{r}
ggplot(datos, aes(x = Fossil.fuels, y = EPI, color = RegionWB)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, color = "gray") +
  labs(
    title = "Relación entre el Combustible y el EPI",
    x = "Balanza de Combustible",
    y = "Índice de Desempeño Ambiental (EPI)"
  ) +
  theme_minimal()
```

Distribución de metal.
```{r}
Metal_region <- datos %>%
  group_by(RegionWB) %>%
  summarise(cantidad = sum(Metal.ores, na.rm = TRUE)) %>%
  arrange(desc(cantidad))
Metal_region

ggplot(Metal_region, aes(x = reorder(RegionWB, cantidad), y = cantidad)) +
  geom_col(fill = "gray") +
  coord_flip() +  # Para que las regiones estén en el eje Y
  labs(
    title = "Metales por región",
    x = "Región",
    y = "Metales"
  ) +
  scale_y_continuous(labels = comma) + 
  theme_minimal()
```

```{r}
ggplot(datos, aes(x = Metal.ores, y = EPI, color = RegionWB)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, color = "gray") +
  labs(
    title = "Relación entre el Metal y el EPI",
    x = "Balanza de Metal",
    y = "Índice de Desempeño Ambiental (EPI)"
  ) +
  theme_minimal()
```

Distribución de productos mixtos y complejos.
```{r}
Mix_region <- datos %>%
  group_by(RegionWB) %>%
  summarise(cantidad = sum(Mixed.and.complex.products.nec., na.rm = TRUE)) %>%
  arrange(desc(cantidad))

ggplot(Mix_region, aes(x = reorder(RegionWB, cantidad), y = cantidad)) +
  geom_col(fill = "pink") +
  coord_flip() +  # Para que las regiones estén en el eje Y
  labs(
    title = "Productos mixtos por región",
    x = "Región",
    y = "Productos mixtos"
  ) +
  scale_y_continuous(labels = comma) + 
  theme_minimal()
```

```{r}
ggplot(datos, aes(x = Mixed.and.complex.products.nec., y = EPI, color = RegionWB)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, color = "gray") +
  labs(
    title = "Relación entre Productos mixtos y el EPI",
    x = "Balanza de Productos",
    y = "Índice de Desempeño Ambiental (EPI)"
  ) +
  theme_minimal()
```

Distribución de no.metales.
```{r}
NoMetal_region <- datos %>%
  group_by(RegionWB) %>%
  summarise(cantidad = sum(Non.metallic.minerals, na.rm = TRUE)) %>%
  arrange(desc(cantidad))
NoMetal_region

ggplot(NoMetal_region, aes(x = reorder(RegionWB, cantidad), y = cantidad)) +
  geom_col(fill = "orange") +
  coord_flip() +  # Para que las regiones estén en el eje Y
  labs(
    title = "No-Metales por región",
    x = "Región",
    y = "No-Metales"
  ) +
  scale_y_continuous(labels = comma) + 
  theme_minimal()
```

```{r}
ggplot(datos, aes(x = Non.metallic.minerals, y = EPI, color = RegionWB)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, color = "gray") +
  labs(
    title = "Relación entre el No-metales y el EPI",
    x = "Balanza de No-metales",
    y = "Índice de Desempeño Ambiental (EPI)"
  ) +
  theme_minimal()
```



Análisis categórico entre las regiones del mundo y el nivel de desempeño ecológico (EPI), usando chi-cuadrado y visualizaciones corrplot.
```{r}
datos <- datos %>%
  mutate(EPI = ntile(EPI, 3), 
         EPI = factor(EPI, levels = 1:3, labels = c("Bajo", "Medio", "Alto"))) #Se convierte la variable numérica en categórica, dividiendola en 3 niveles.
tabla <- table(datos$RegionWB, datos$EPI) #Creación de tabla que contenga la Región y EPI. 
chi2 <- chisq.test(tabla) #chi-cuadrado para evaluar si hay relación entre región y nivel del EPI
chi2
corrplot(chi2$residuals, is.cor = FALSE, main = "Región vs EPI") #Visualización de residuos de chi-cuadrado
```
X-cuadrado (estadístico de prueba) : 81.67 → mide cuán diferentes son las frecuencias observadas vs. esperadas.
df (grados de libertad) : 12.
Valor p : 0.00000000000198 → extremadamente pequeño, lo que dice que si hay una relación significativa entre cada región y el EPI. La distribución del EPI no es igual en todas las regiones 

Este gráfico muestra los residuos del chi-cuadrado , que te dicen qué celdas específicas contribuyen más a la relación detectada.
Círculos azules : Hay más países de los esperados en esa combinación.
Círculos rojos : Hay menos países de los esperados .
Tamaño y color indican qué tan grande es la diferencia entre lo observado y lo esperado.






